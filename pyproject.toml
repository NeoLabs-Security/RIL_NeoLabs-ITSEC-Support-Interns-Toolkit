[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "neolabs-itsec-support-intern-cli"
version = "0.1.0"
description = "NeoLabs × RIL IT Security Support internship pod access client"
requires-python = ">=3.10"

[project.scripts]
neolabs = "tools.neolabs:main"

[tool.setuptools]
packages = ["tools"]
